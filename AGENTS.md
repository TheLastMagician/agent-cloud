## Cursor Cloud specific instructions

This repository is currently empty (contains only a placeholder file `1`). There are no application services, dependencies, build steps, lint configurations, or tests to run.

When the repository gains actual application code, this section should be updated with:
- How to install dependencies
- How to run, build, lint, and test the application
- Any non-obvious caveats about the development environment
